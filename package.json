{
    "name": "root",
    "private": true,
    "scripts": {
        "release": "lerna publish --force-publish=*",
        "release:alpha": "lerna publish --force-publish=* --npm-tag=alpha"
    },
    "devDependencies": {
        "lerna": "^3.4.3"
    }
}
